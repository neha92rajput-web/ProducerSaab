'use client';

import React from 'react';

export const AudioPlayer: React.FC = () => {
  // Legacy Audio Player component bypassed in favor of self-contained synthesis engine in AudioDashboard.tsx
  return null;
};
