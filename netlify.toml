```toml
[build]
  command = "npm run build"
  publish = ".next"

[plugins]
  package = "@netlify/plugin-nextjs"

[[edge_functions]]
  path = "/*"
  excluded_paths = ["/", "/signin", "/dashboard", "/library", "/feed"]
